# Pre-release Readiness Audit

Date: 2026-07-24

## Purpose

This audit is the release gate before artifact-publication work begins. It aligns the supported product contract, release documentation and package-facing material; validates the complete functional surface; examines security and trust boundaries; and identifies final product polish.

The audit covers the current source state. Historical plans and implementation evidence remain development records and are not treated as release promises.

## Preliminary findings

The initial inventory identified these actionable inconsistencies:

- the root README contains a development-only `Next` note about nullable Code Action selections;
- the Plugins package README links to a diagnostics document that is not included in the package and does not use a durable public URL;
- the Workspace support package uses the Plugins authoring guide as its package README even though it is not the author-facing package;
- the Code Action audit workflow still watches the former release-document location instead of `docs/development/RoslynCodeActionsAudit.md`;
- the public platform-support text describes macOS release validation before that validation workflow exists;
- `FutureTasks.md` retained completed acceptance and analyser work despite its active-backlog policy; and
- no final public API compatibility, security or release-polish review has been recorded for the release candidate.

These are inventory findings, not the complete audit result.

## Functionality findings promoted from public documentation

| ID | Status | Finding | Disposition |
| --- | --- | --- | --- |
| `PRR-F001` | Resolved | Location-driven Code Action requests such as `AddAwaitRequest` exposed a nullable `Selection`, while the common staging path rejected a missing selector. | All 20 execution-required Code Action target properties are now required and non-nullable through the published request, resolution and staging paths. Schema contract coverage verifies that each property is required and excludes `null`; request binding also rejects explicit `null` for non-nullable properties. |
| `PRR-F002` | Resolved as unverified best effort | Public planning classified macOS as best effort, but `FileStreamWorkspaceFileLockProvider` rejected commit locking outside Windows and Linux. | The macOS path now uses Apple `flock(2)` through `libSystem.dylib` because .NET 10 marks `FileStream.Lock` and `Unlock` unsupported on macOS. Atomic replacement retains the same-directory `File.Move` design and uses macOS `libSystem` imports for directory `open` and `fsync`. This is a research-backed best guess, not validated support; the existing cross-process lock, atomic-write and durability integration coverage must run on a hosted macOS agent during public release-candidate preparation. |
| `PRR-F003` | Resolved | Plugin search roots were deduplicated with `StringComparer.OrdinalIgnoreCase` on every platform before plugin discovery. Linux and macOS can contain distinct roots whose paths differ only by case, so one configured root could be discarded even though the public configuration contract says command-line and environment roots are combined and deduplicated. | Startup configuration now uses the existing filesystem-aware Workspace path comparison for each root. This preserves case-distinct roots on native Linux and macOS while retaining case-insensitive deduplication on Windows and Windows-mounted WSL paths; resolver evidence covers both comparison modes. |
| `PRR-F004` | Resolved | `--code-action-token-lifetime` accepted every positive `TimeSpan`, but Code Action discovery calculates expiry with `DateTimeOffset.Add`. A sufficiently large accepted duration could exceed `DateTimeOffset.MaxValue` and make discovery throw instead of producing an action token. | The supported lifetime is now bounded to 24 hours, the release configuration publishes that maximum, excessive values use the existing fallback-warning contract, validation uses the same rule, and Code Action coverage proves expiry calculation at the accepted maximum. |
| `PRR-F005` | Resolved | Workspace path identity was compared with `StringComparison.Ordinal` during duplicate-open checks, path selection and pending-recovery matching. Native Windows and the default Windows-mounted WSL filesystem are case-insensitive, so a casing variant could fail selection, open a duplicate session or bypass the Workspace-level recovery preflight. | Duplicate-open, path-selector and pending-recovery checks now use `IWorkspacePathComparison` for filesystem paths while generated IDs and caller aliases remain ordinal. Unit evidence covers both case-sensitive and case-insensitive policies across all three paths. |
| `PRR-F006` | Resolved | `WorkspaceResolver` derived its relative-path base from the loaded solution or project directory instead of the published `WorkspaceIdentity.WorkspaceRoot`. When an explicit root was above the loaded path, emitted document and project references were not Workspace-relative and documented Workspace-relative selectors did not resolve consistently. | Resolution and projection now use `WorkspaceIdentity.WorkspaceRoot`, with filesystem-aware path comparison. Unit and component evidence places the Workspace root above the solution and project directories and proves project and document references round-trip through selectors. |
| `PRR-F007` | Resolved | External-change state transitions updated the in-memory Workspace session but did not update the advisory cross-instance status record. Another Host could therefore continue to observe `Ready` or `TransactionActive` after the owning Host transitioned to `WorkspaceOutOfDate` or `TransactionConflicted`. | Every external-change transition now queues an advisory status update through a publisher-owned ordered worker. Awaited transaction publications share the same queue, shutdown drains queued updates, and status publication remains non-blocking for mutation decisions. Unit evidence covers queue ordering and lifecycle ownership; two-instance component evidence observes both `WorkspaceOutOfDate` and `TransactionConflicted`. |
| `PRR-F008` | Resolved | External-change detection identified files only by existence, length and last-write time. A same-length edit whose timestamp was preserved or fell within a coarse filesystem timestamp interval was treated as unchanged, allowing queries to continue against a stale Roslyn snapshot despite the documented reload contract. | Each loaded Workspace now owns a recursive filesystem change monitor scoped to its manifest files and input directories, while the existing file metadata polling remains as a fallback. Monitoring starts only after the manifest is known, detects tracked-file changes plus relevant create, delete, rename and watcher-error events, ignores directory timestamp churn, and retains the first trigger for `workspace-status`. Generated trees are identified from each physical project's evaluated MSBuild output and intermediate properties, Roslyn's output paths and the Workspace-owned `.vs` state root instead of excluding every directory named `bin` or `obj`. Exact manifest inputs remain monitored even when they are generated beneath an excluded root. Unsafe evaluated roots that contain a loaded solution or project are rejected, and a project-local `bin`/`obj` fallback is used only when evaluation supplies no output root. Native callbacks hand lightweight notifications to an ordered unbounded channel backed by the maximum 64 KiB watcher buffer; filtering and first-change publication run on one consumer, while request-time validation synchronises with notifications already accepted by managed code. The monitor stops after the first relevant change and is disposed on close, reload, commit replacement and failed loading. Real-filesystem, published-host and repository-scale evidence covers same-length timestamp-preserving edits, custom and central artifact layouts, actionable diagnostics, an 80,000-operation watcher burst and three consecutive native Windows EF Core live builds without overflow or false staleness. |
| `PRR-F009` | Resolved | Twenty-four bundled query requests published their sole execution-required document, project, symbol or location selector as nullable and optional. The handler then rejected its absence even though the equivalent Code Action contracts already made execution-required selectors schema-required and non-nullable. | The 24 selectors are now `required` and non-nullable. Alternative-target selectors, optional narrowing scopes and requests with defined solution-scope semantics remain nullable. Unreachable handler-level missing-selector branches and tests were removed; schema and MCP binding contract tests now prove required, non-nullable publication and missing-member rejection. The shared binding pipeline returns the standard `InvalidRequest` envelope with a concise message naming all omitted arguments, while the shared plugin request resolver retains nullable rejection behaviour for external contracts. |
| `PRR-F010` | Resolved | `GetSymbolInfoRequest.IncludeMembers` was published but never read, and `SymbolInfoData` has no members field. Setting the property could not affect the response. | The misleading property has been removed from the request contract and contract catalogue. Member enumeration remains the responsibility of the separately published `get-symbol-members` tool. |
| `PRR-F011` | Resolved | `get-api-surface` and `get-partial-declarations` fully discovered their eligible ordered result sets but published only `HasMore`, even though the complete count was already available without additional discovery. | Both tools now pass the known eligible count to `BoundedCollection.CreatePrebounded`; focused tests verify the published total and bounded prefix. |
| `PRR-F012` | Resolved | Seven execution-required bundled mutation arguments were published as optional even though the handlers or resolvers could not execute without them: the document targets for formatting and using sorting, the symbol and new name for rename, and the action tokens for action, fix and fix-all staging. | The seven properties are now `required` and non-nullable. Schema contract evidence verifies required non-nullable publication, and the shared binder reports the omitted argument names for each real mutation request contract. |
| `PRR-F013` | Resolved | Seven dedicated Code Action mutation handlers treated an undefined numeric enum value as one of their supported default operations, allowing malformed input to stage a different refactoring than the caller requested. The eight operation-selecting Code Action enum arguments also relied on implicit zero-value defaults rather than requiring caller intent. | The shared MCP request binder now caches top-level enum metadata and rejects supplied undefined values before any built-in, Code Action or external-plugin handler executes. All eight operation-selecting Code Action enums are required and non-nullable, so omission is also rejected rather than inferred. Nullable values, omitted genuinely optional arguments and valid flag combinations retain their normal semantics. Real request-contract coverage includes all eight Code Action enum arguments plus server-status and transaction-history enums. |
| `PRR-F014` | Resolved | `stage-fix-all.maxChanges` published a curated default of 50, but an explicit JSON `null` reached the stager as no limit and disabled the safety bound. | `StageFixAllRequest.EffectiveMaxChanges` now maps omission and explicit `null` to 50 and clamps negative values to zero. The stager consumes only the non-nullable effective limit; focused tests distinguish the default boundary, explicit lower limits and negative input. |
| `PRR-F015` | Resolved | Mutation snapshot preconditions were nullable and independently declared across bundled, Code Action and transaction requests. A missing or explicit-null value could therefore bypass the snapshot guard even though mutation execution relies on an active transaction snapshot. | `WorkspaceMutationRequest` now owns a required, non-null `ExpectedSnapshot` precondition for every Workspace mutation. Plugin and Code Action mutation registrations require that base contract, internal staging requests retain the same non-null invariant, and schema evidence verifies the inherited property is required and excludes `null` for every built-in mutation request. |
| `PRR-F016` | Resolved | `format-document` constructed a Roslyn `TextSpan` and invoked the formatter without validating that the caller-supplied range was non-negative and contained by the selected document. Malformed input could therefore escape the normal structured rejection path. | The handler now validates start, length and end against the selected document text before constructing the span and returns `InvalidRequest` for an invalid range. Boundary coverage includes negative values, starts and ends beyond the document, and overflow-resistant large input. |
| `PRR-F017` | Resolved | `sort-usings` classified every namespace beginning with the text `System` as a System namespace, so a namespace such as `Systematic` was incorrectly promoted when `systemFirst` was enabled. | Classification now requires the exact `System` namespace or a `System.` prefix, normalises the `global::` alias for classification and ordering, and retains deterministic ordinal ordering. Focused evidence distinguishes `Systematic`, `System.Text` and `global::System`. |
| `PRR-F018` | Resolved | `transaction-history.direction` selected the operation to perform but was optional, so omission silently selected the zero-valued `Undo` operation. | The direction is now required and non-nullable. The shared binder names it alongside the inherited snapshot precondition when omitted and retains the existing undefined-enum rejection. |
| `PRR-F019` | Resolved | `transaction-preview` returned a successful transaction summary with no diff when `includeDiff` was true but the document selector was missing, ambiguous, unresolved or could not be represented in the Workspace. | Diff requests now require a document selector and reject every unresolved target through structured `InvalidRequest`, `DocumentNotFound` or `DocumentAmbiguous` results with target-resolution guidance. Resolution occurs before change-summary construction so invalid requests do not perform unnecessary diff work. |

## Batch 1 — Documentation and release-surface alignment

**Status:** Complete

1. Remove development-only notes, implementation instructions and stale future claims from release-facing documents.
2. Reconcile terminology, supported-platform statements, links, examples, CLI options, tool behaviour and transaction guidance with the current implementation.
3. Give every published package an appropriate self-contained README and durable diagnostic links.
4. Repair workflow documentation paths and check all release-document links.
5. Keep active work in `FutureTasks.md`; retain completed evidence only in the relevant audit or results document.
6. Decide whether public repository files such as `SECURITY.md`, `CONTRIBUTING.md`, a code of conduct and support guidance are required for v1.

Batch 1 establishes the written product contract used by the remaining audits.

The root and release-document indexes now describe the product without an embedded development backlog. Workspace loading, WSL storage guidance, bounded defaults and current platform support are aligned with the implementation. The stale Code Action workflow paths and package-management paths are corrected.

The Plugins package retains the authoring guide as its README and uses the same durable diagnostics URL as the analyser descriptors. The transitive Workspace support package now has a separate README that explains its dependency role without presenting it as the author-facing package. Package integration coverage inspects both generated READMEs.

`SECURITY.md` and `CONTRIBUTING.md` now provide the required private-reporting boundary and contribution entry point. A separate `SUPPORT.md` is unnecessary for v1 because the release documentation and issue guidance already define those routes. A code of conduct is a repository-owner community-governance choice rather than a product security control; select one before actively soliciting public contributions, but it does not block technical release validation.

## Batch 2 — Supported-functionality and contract audit

**Status:** Complete

Validate the implementation against the written product contract:

- executable startup, command-line validation, configuration precedence, help and version behaviour;
- workspace open, selection, diagnostics, stale-state handling, reload and close behaviour;
- every published query, mutation and Code Action tool, including complete request-property mapping;
- schema descriptions, curated defaults, selectors, bounded collections, known totals and result invariants;
- transaction staging, preview, commit, rollback, conflict, recovery, restart and multi-workspace ownership;
- unsupported project handling and Windows, Linux and WSL path behaviour;
- plugin discovery, authoring, validation, analyser activation, package layout and failure containment; and
- live tool discovery and acceptance coverage against the actual published Host.

Record each finding with its affected contract, severity, evidence and required disposition. Group safe fixes, but isolate changes that affect public contracts, persistence, transactions or compatibility.

### Host startup, configuration and lifecycle validation

The validation used the release-facing [Getting started](../GettingStarted.md), [Configuration](../Configuration.md) and [Tool discovery and results](../ToolDiscovery.md) documents as the product contract. Existing implementation evidence came from the configuration and status unit tests, Host composition and startup-prerequisite integration tests, and the 41-case published-Host acceptance suite recorded in the [Published Host Acceptance Coverage Audit](AcceptanceCoverageAudit-2026-07-23.md).

| Contract area | Result | Evidence and disposition |
| --- | --- | --- |
| Published executable startup and stdio transport | Validated | The acceptance fixture launches the exact configured Release-published executable, initialises through the official MCP client and exercises the public catalogue and `server-status`. Host composition registers stdio transport and all server-owned, bundled, Code Action and external-plugin tool families. |
| Protocol and operational stream separation | Validated | Host logging clears inherited providers and routes every configured log level to stderr; MCP stdio transport owns stdin/stdout. Published startup failure diagnostics are captured from stderr without treating them as protocol messages. |
| Graceful process lifetime | Validated | Direct published-process acceptance closes stdin, observes process exit code `0` and retains a bounded forced-cleanup fallback only for the known MCP SDK client-disposal limitation. Normal acceptance disposal verifies process completion and isolated-root cleanup. |
| Startup without a Workspace | Validated | Published protocol and catalogue/status acceptance run before any Workspace is opened, and the live catalogue remains stable before open, during a transaction and after close. |
| Documented options, environment variables and defaults | Validated | Resolver tests cover every documented option in separated and `=` forms, every environment variable, all documented defaults, supported value ranges, schema modes, state-directory syntax and plugin-root collection. The token-lifetime range discrepancy found during validation was resolved under `PRR-F004`. |
| Scalar precedence, repeatable roots and invalid-value fallback | Validated | Unit and published acceptance prove command-line scalar precedence, last-value semantics, combined command-line/environment plugin roots, fallback warnings and effective status projection. Filesystem-aware plugin-root deduplication was resolved under `PRR-F003`. |
| Runtime propagation of configuration | Validated | Host composition tests prove that result, concurrency and transaction limits reach `WorkspaceOptions`, token lifetime reaches `CodeActionExecutionOptions`, and state-directory and schema-mode values reach their owning services. Published acceptance exercises configured single-slot query concurrency and default/full schema publication. |
| Non-sensitive effective status and startup diagnostics | Validated | Full `server-status` acceptance verifies effective non-sensitive values, fallback warnings, actionable MSBuild status and omission of state, plugin and scenario paths. Standard detail omits expanded configuration, plugin and recovery branches. |
| Startup prerequisite ordering and recovery | Validated | Component integration proves MSBuild registration and commit recovery complete before the transport starts. Published restart acceptance reports durable recovery conflict state and blocks affected Workspace opening with the documented recovery action. |
| Startup-fixed plugin catalogue | Validated | Published acceptance covers combined package roots, valid and invalid package isolation, collision handling, sanitised configuration failures and discovery of a newly installed package only after restart. |
| Help, version and unknown-argument behaviour | No published contract | Release documentation does not advertise standalone help or version commands or rejection of unknown arguments. Product and Roslyn versions are observable through `server-status`, and the resolver deliberately ignores unrelated arguments. No release discrepancy is recorded; exact GitVersion-derived artifact identity remains part of release publication validation. |
| Supported platforms | Validated for available evidence | Native Windows and WSL/Linux published acceptance evidence is recorded. macOS implementation is resolved as unverified best effort under `PRR-F002`; hosted macOS validation remains deferred until public release-candidate preparation. |

### Workspace lifecycle and stale-state validation

The validation used [Workspaces and transactions](../WorkspacesAndTransactions.md), the first-workflow guidance in [Getting started](../GettingStarted.md), and the selector contract retained in the development design records. Implementation evidence came from the Workspace lifecycle, loading, selection, resolution, change-detection, coordination and execution-context services; their unit and component integration suites; and the published-Host acceptance evidence recorded in the [Published Host Acceptance Coverage Audit](AcceptanceCoverageAudit-2026-07-23.md).

The completed non-acceptance validation passed all 795 Workspace fast unit and contract cases, all 71 Workspace integration cases and all 283 Host unit and component cases. The complete published-Host acceptance suite passes all 41 cases on both WSL/Linux and native Windows, including linked multi-target selection and mutation, actionable external-change diagnostics and generated-output changes that must not make the Workspace stale.

| Contract area | Result | Evidence and disposition |
| --- | --- | --- |
| Absolute `.csproj`, `.sln` and `.slnx` loading | Validated | The loader normalises only absolute supported extensions and dispatches projects separately from solutions. Published acceptance opens all three forms. |
| Supported-project filtering and load diagnostics | Validated | Unsupported languages, missing project paths, non-SDK projects and unresolved analyser references are removed with structured diagnostics. Loading rejects when no supported SDK-style C# project remains. Unit, component and published acceptance cover usable mixed input and malformed or unsupported rejection. |
| Workspace-root selection and containment | Validated for lexical paths | Explicit roots must be existing absolute directories containing the loaded path, and every retained project and source document is checked against that root. Symlink and canonical-boundary resistance belongs to Batch 3. |
| Workspace identity and path comparison | Resolved | Duplicate-open, path-selector and pending-recovery checks consistently use the filesystem-aware comparison policy. Case-sensitive and case-insensitive evidence resolves high-severity `PRR-F005`. |
| Workspace-relative project and document selectors | Resolved | ID, alias, canonical-path, implicit, project-qualified and ambiguous selector paths retain their published behaviour, while relative project and document paths now use the selected Workspace root and round-trip when the root is above the loaded path. This resolves medium-severity `PRR-F006`. |
| Multiple Workspace routing and ownership | Validated | Omitting a selector succeeds for one Workspace and rejects for many; Workspace gates remain independent; one global transaction owner is projected by `workspace-list`; closing one Workspace leaves another queryable. |
| Duplicate open, capacity and unsafe close | Validated | Structured errors cover duplicate identity under the effective filesystem casing policy, maximum loaded Workspace capacity, busy acquisition and close while a transaction is active. |
| External source and configuration changes | Resolved | Source additions, changed source, `Directory.Build.props` and editor-config changes transition the session and reject subsequent work with reload guidance. Manifest-scoped filesystem monitoring also detects same-length, timestamp-preserving edits while retaining metadata polling. Generated documents are excluded through evaluated project output/intermediate roots plus the Workspace `.vs` state root, while ordinary source trees merely named `bin` or `obj` remain tracked. Exact generated MSBuild inputs remain authoritative, and `workspace-status` retains the first trigger for diagnosis. Channel-backed event handling with a 64 KiB native buffer remained healthy through an 80,000-operation Windows stress run and three consecutive EF Core live builds. Published acceptance and repository-scale scenarios resolve medium-severity `PRR-F008`. |
| Reload and snapshot invalidation | Validated | Reload is restricted to out-of-date Workspaces without an active or conflicted transaction, creates a new epoch, replaces and disposes the old loaded Workspace, invalidates query-cache state and causes old snapshot preconditions to reject. Published acceptance proves refreshed semantic results. |
| Close and resource cleanup | Validated | Close takes exclusive access, rejects an open transaction, removes and cache-invalidates the selected session, closes its advisory status handle and disposes its loaded Roslyn Workspace. Component and published acceptance cover empty and multi-Workspace aftermath. |
| Cross-instance advisory state | Resolved | Open and status surface unavailable, unreadable and live-instance warnings with the documented query-only guidance. Transaction and external-change transitions publish through one ordered, lifecycle-owned queue; two-instance evidence covers out-of-date and conflicted observations. This resolves low-severity `PRR-F007`, and advisory state remains intentionally non-blocking. |
| WSL-mounted Windows storage warning | Validated | `workspace-open` adds a non-persisted `WorkspaceOnWindowsFileSystemFromWsl` warning using mount-aware path classification. Native WSL/manual release evidence remains the appropriate validation tier. |

### Published query-tool validation

The query audit covers the 38 bundled inspection handlers, the two internal Code Action query handlers and their Host publication paths. The authoritative catalogue is the materialised built-in registration rather than the historical design catalogue. Existing evidence includes the complete bundled registration contract, dedicated handler tests, the comprehensive tool-coverage ledger, schema-default integration coverage, selector and snapshot component coverage, all four Host execution adapters and representative published-Host workflows.

| Contract area | Result | Evidence and disposition |
| --- | --- | --- |
| Published query inventory and metadata | Validated | `BundledCorePluginTests` locks all 38 bundled query names, descriptions and read-only behaviour. Code Action registry tests lock the two internal query registrations, duplicate-name rejection and separation from plugin status. Host composition and acceptance evidence prove materialisation through the production catalogue. |
| Request-property propagation | Resolved | A property-to-handler audit found every bundled query property consumed directly or through its named effective limit except the ineffective `GetSymbolInfoRequest.IncludeMembers`, which was removed under `PRR-F010`. Both Code Action query properties are consumed. The mutation request families remain in the next audit scope. |
| Required selector shape | Resolved | The 24 query requests with exactly one valid execution target now publish that selector as required and non-nullable under `PRR-F009`. Alternative-target requests such as `find-callees` and `get-control-flow-graph`, optional narrowing scopes, and scope-or-symbol requests with defined solution defaults retain their nullable shapes. Contract evidence audits all 24 published schemas, and the generic MCP binder rejects omitted required members through the standard `InvalidRequest` envelope with a concise message naming every missing argument. |
| Curated defaults and effective limits | Validated | The schema-default contract dynamically inspects every built-in integer limit across Plugins.Core, CodeActions and Host transaction contracts, requires `DefaultValueAttribute`, verifies the published integer default and verifies default request construction. Bundled request types retain private curated constants and named effective-limit properties. |
| Deterministic bounds and known totals | Resolved | Every explicitly bounded bundled collection uses `BoundedCollection.CreatePrebounded`; handler and scenario evidence covers zero, low, default, high and above-result-count limits, deterministic prefixes and `HasMore`. `get-api-surface` and `get-partial-declarations` now publish their cheaply available complete counts under `PRR-F011`. `list-code-actions` remains the documented intentional exception that returns the complete applicable action set for one location. |
| Selector and snapshot semantics | Validated | Shared resolver tests cover document, project, scope and symbol outcomes; component tests cover project-qualified selectors, ambiguity and stale snapshots. Location-based queries pass snapshot preconditions before resolving current source. |
| Result invariants and transport mapping | Validated | `PluginExecutionResult`, `CodeActionExecutionResult`, `ToolResolutionResult` and `BoundedCollection` factories make invalid success/error and count combinations unrepresentable. The query Host adapters cover success, rejection, conflict, failure, cancellation, malformed binding and exception sanitisation at full line and branch coverage. |
| Query algorithm and Roslyn boundary behaviour | Validated for supported paths | Every production query handler has dedicated tests, while real-Workspace component suites cover projection, semantic inspection, cross-project search, selector/snapshot behaviour and controlled Code Action discovery. The comprehensive coverage ledger records only approved defensive Roslyn-shape alternatives. |
| Published execution evidence | Validated representatively | Acceptance covers server-owned queries, bundled semantic queries, external plugin queries and Code Action discovery through the published executable. The release scenario suite supplies repository-scale bounds, determinism, cache, cancellation and performance evidence without making acceptance one test per tool. |

### Published mutation-tool validation

The mutation audit covers the three bundled mutation handlers, 46 dedicated or token-driven Code Action mutation handlers, the external-plugin mutation adapter and the shared Workspace staging boundary. Request-property propagation and staging ownership are validated. Required mutation arguments, explicit Code Action operation choices, undefined enum values, the fix-all effective safety limit and mandatory snapshot preconditions are resolved under `PRR-F012`, `PRR-F013`, `PRR-F014` and `PRR-F015`. Range formatting validation and System-namespace using classification are resolved under `PRR-F016` and `PRR-F017`. Independent result assertions now prove `renameOverloads`, `renameInStrings`, `renameInComments` and `renameFile`; interface-member evidence confirms Roslyn's standard implementation propagation, which is not a separate `SymbolRenameOptions` choice.

### Transaction validation

The transaction audit covers all five server-owned transaction tools, the shared staging service, revision history, transaction ownership, durable planning and writing, native commit locking, conflict recovery and restart recovery.

| Contract area | Result | Evidence and disposition |
| --- | --- | --- |
| Published inventory, metadata and request propagation | Resolved | All five registrations have the documented read-only and destructive hints and pass every request property to `ITransactionService`. `transaction-history.direction` is now required under `PRR-F018`; commit and history inherit the mandatory snapshot precondition, while start, preview and rollback remain intentionally non-snapshot operations. |
| Preview and document targeting | Resolved | Summary preview remains available without a document. A requested detailed diff now requires and resolves an explicit document instead of silently returning no diff; `PRR-F019` records the structured failure paths. Context lines retain the documented default and the diff builder deliberately clamps non-positive values to zero. |
| Staging, revision bounds and history | Validated | Mutation candidates are checked against the active transaction and Workspace boundary, linked-document changes are merged, no-change proposals do not consume revisions, configured revision capacity is enforced, and staging after undo discards the abandoned redo branch. Unit, component and published acceptance exercise rejected, successful, undo and redo flows. |
| Ownership and lifecycle state | Validated | A single Host transaction owner is enforced across loaded Workspaces, advisory cross-instance state does not claim mutation exclusion, and start, rollback and commit publish ordered state changes. Published acceptance transfers ownership between two Workspaces and verifies the owner projection. |
| Commit validation and external drift | Validated | Commit requires the expected snapshot, rejects conflicted or externally changed sessions, acquires the cross-process Workspace lock, persists the plan, revalidates targets under the lock and revalidates each entry immediately before replacement. Conflict scenarios retain only the external change and leave the transaction recoverable. |
| Durable application and restart recovery | Validated | Same-directory atomic replacement, backups, delete markers, manifests and recovery status protect create, replace and delete operations. Applying is deliberately non-cancellable after the durable boundary; cancellation before application restores staged artifacts. Unit, native integration, published acceptance and repository-scale crash scenarios cover recovery, recovery conflict and normal cleanup. |
| Commit promotion and cleanup | Validated | Successful commit promotes the staged immutable solution, rebuilds the input manifest, clears transaction ownership and removes recoverable state. Failure paths retain or restore sufficient state for retry or startup recovery, and all validated scenario runs finish at the pinned repository commit with no unexpected state files. |

### Plugin validation

The plugin audit covers the public authoring API, analyser delivery, package discovery, metadata inspection, isolated dependency loading, configuration and handler validation, deterministic collision policy, Host materialisation and runtime failure containment.

| Contract area | Result | Evidence and disposition |
| --- | --- | --- |
| Public authoring surface and package dependency shape | Validated | The exported Plugins API is locked to the documented entry point, configuration, handler, context, result, selector and analysis contracts. Workspace contracts arrive transitively, Host staging is absent, execution-context properties are get-only and the Plugins package contains the dedicated analyser without embedding Roslyn implementation assemblies. |
| Clean consumer and analyser activation | Validated | Package integration packs Workspace and Plugins, inspects both package layouts, restores a clean external consumer and proves `RWMCP001`–`RWMCP019` activate from the package-delivered analyser. The analyser project retains its isolated dependency policy. |
| Package discovery and containment | Validated | Discovery inspects only immediate package directories and top-level DLLs, reads entry metadata without loading candidate code, requires exactly one marked entry point and rejects assembly or dependency paths escaping the package through lexical or resolved-link containment. Missing, malformed and inaccessible packages are isolated as disabled statuses. |
| Metadata, API compatibility and composition | Validated | Plugin identity fields, NuGet-semantic informational versions and the exact v1 API string are checked before loading. MEF composition requires the single declared plugin entry point, while configuration exceptions and unexpected loading failures disable only the affected plugin with sanitised diagnostic types. |
| Handler and transport contracts | Validated | Compile-time generic constraints and the shipped analyser guide normal authoring; runtime validation remains authoritative for handler family, construction, lifetime, imports, public external request/response contracts, metadata, duplicate names and unsafe state warnings. Any error disables the plugin atomically. |
| Dependency identity and isolation | Validated | One non-collectible load context and dependency resolver is created per valid external package. Plugins, Workspace, Roslyn and composition identities are shared from the Host; other managed and native dependencies remain package-local and are containment checked. Published acceptance proves a private dependency loads successfully. |
| Collision policy and fixed catalogue | Validated | Server-owned, Code Action and bundled names are protected; every package sharing an external plugin ID and every external plugin sharing a tool name is disabled deterministically. The catalogue is fixed after startup and published acceptance proves a newly installed package appears only after restart. |
| Query and mutation execution containment | Validated | Typed binding and Workspace acquisition precede invocation, handler outcomes retain structured diagnostics and recovery actions, mutation candidates pass only through Host staging, leases are always disposed, cancellation propagates, and unexpected handler failures are sanitised by the common MCP boundary. Deliberate live-Workspace mutation is detected after query or mutation execution and transitions through the existing stale/conflicted recovery path. |
| Trusted in-process boundary | Validated as documented residual risk | Public contexts expose an immutable `Solution` snapshot and no stager or Host service, and runtime containment detects accidental `Workspace.TryApplyChanges` use. The associated Roslyn Workspace, filesystem, process and reflection APIs remain reachable to trusted in-process code; the release documentation accurately states that adversarial isolation is not provided and untrusted plugins must not be loaded. |

## Batch 3 — Security and trust-boundary audit

Create an explicit threat model for the local stdio Host and its trusted in-process plugin model. Audit:

- command-line, MCP, JSON and plugin metadata input validation;
- path canonicalisation, root containment, traversal, symlinks and Windows/WSL path translation;
- transaction time-of-check/time-of-use behaviour, atomic replacement, journals and crash recovery;
- state and temporary-directory permissions, cleanup and sensitive source retention;
- process invocation, MSBuild arguments, environment inheritance and command-injection resistance;
- plugin discovery, dependency loading, identity, collisions and the documented absence of adversarial isolation;
- resource-exhaustion controls for large workspaces, compilations, results and concurrent requests;
- cancellation and shutdown behaviour at safe and unsafe interruption points;
- stdout protocol integrity, stderr logging, error disclosure and accidental secret or source leakage;
- NuGet dependencies, known vulnerabilities, generated package contents and analyzer dependency isolation; and
- GitHub Actions permissions, third-party action pinning, artifact handling and future publishing credentials.

Use static analysis and dependency tooling as evidence, not as a substitute for design review. Fix exploitable or boundary-breaking findings before release. Document accepted residual risks in release-facing security guidance.

### Threat model

Roslyn Workbench runs locally under the user's operating-system identity, against repositories selected by an MCP client acting under the user's or an agent's instructions. The MCP client is authorised but fallible: it may supply malformed input, make an incorrect selection or follow hostile instructions found in repository content. It is not treated as a hostile remote tenant, and the Host is not expected to defend against arbitrary code already executing as the same operating-system user.

The Workspace root is an enforced mutation boundary, not merely an organisational hint. An agent-directed mutation must not read or write through a symlink, junction or other resolved path to a target outside that boundary. Concurrent editors, builds and other Roslyn Workbench instances are expected operational actors; non-cooperating processes are not isolated by the Workspace commit lock.

Repositories are not trusted merely because they are selected by the user or already present on the user's machine. Opening a solution or project evaluates MSBuild logic, and later diagnostic or Code Action operations may load and execute package-provided analyzers and generators. Microsoft therefore advises running or opening MSBuild projects only from fully trusted sources in its [secure MSBuild usage guidance](https://learn.microsoft.com/visualstudio/msbuild/msbuild-security-best-practices?view=visualstudio). This trust requirement must be visible to the agent before `workspace-open` executes.

Third-party plugins remain explicitly trusted, unsandboxed in-process code. Package and API containment prevents accidental coupling and path escape during discovery, but it does not attempt to isolate a malicious plugin from the user's files, processes, network or Roslyn objects.

### Audit evidence

- The Host and its production dependencies contain no direct external-process invocation or constructed shell command. MSBuild execution occurs through `MSBuildWorkspace` and inherits the Host environment, so repository trust remains the governing control.
- Stdout remains reserved for MCP transport; Host logging is routed to stderr, and unexpected tool exceptions return a correlation identifier rather than exception details.
- Plugin package discovery validates metadata before loading, resolves package-local links, contains managed and native dependencies, shares only required Host identities and sanitises plugin loading and execution failures.
- Cancellation is honoured during cancellable preparation and ignored once durable commit application begins, allowing commit or recovery to reach a terminal state.
- `dotnet list Roslyn.Workbench.Mcp.slnx package --vulnerable --include-transitive --no-restore` reported no known vulnerable direct or transitive dependency from the configured NuGet sources.
- The Host project and its production dependencies completed a `latest-all` analyzer build with no warnings, and the repository scan found no obvious committed credential or private-key material.
- GitHub Actions use repository `contents: read`, do not use `pull_request_target` and expose no publishing secret to pull-request builds. Reusable actions are currently referenced through mutable version tags.

### Findings

| ID | Severity | Status | Finding | Required resolution |
| --- | --- | --- | --- | --- |
| `PRR-F020` | High | Resolved | Workspace loading, mutation validation, commit planning and persisted recovery previously validated lexical absolute paths but did not resolve symlinks or junctions before applying the Workspace-root boundary. A path beneath the root could therefore resolve to a physical target outside it. | `PhysicalPathContainment` now provides the Workspace-owned policy, resolves every existing path segment through final symbolic-link or junction targets and fails closed on resolution errors. Workspace loading, mutation-candidate validation, commit planning, commit application and restoration, recovery manifests and artifacts, plugin-package containment and Workspace-owned `.vs` lock/status coordination use the shared policy. Unit coverage verifies lexical traversal, root equality, contained links, escaping links, resolution failures and rejection at mutation, commit, recovery and coordination boundaries; the durable commit/recovery and Host integration suites remain green with the shared policy. Published-Host acceptance coverage verifies rejection of an escaping source link, use of a contained source link and rejection of escaping `.vs` status/lock writes. The focused acceptance suite passed under WSL using real symbolic links and under native Windows using directory junctions. |
| `PRR-F021` | High | Resolved | Release-facing security guidance previously documented trusted plugins but not executable repository content. `workspace-open` evaluates MSBuild logic before an agent can inspect every imported project input, and diagnostic or Code Action execution can load project analyzers. | The `workspace-open` description now warns agents before execution that only fully trusted workspaces may be opened and identifies MSBuild and analyzer execution with Host permissions. `SECURITY.md`, Getting Started and Workspaces and Transactions carry the same boundary and direct untrusted inspection to an operating-system sandbox. No mechanical trust-confirmation property was added. Unit coverage locks the published tool description. |
| `PRR-F022` | Medium | Resolved | The default recovery root was the predictable shared temporary path `<temporary-directory>/roslyn-workbench-mcp-state`. On the audited WSL environment it was created as `0755`; ordinary recovery files would inherit group/other readability under the common `0022` umask. This could disclose retained source to another Unix user and permitted unsafe pre-creation of the recovery root. | State now defaults to the platform's per-user application-state location: local application data on Windows and macOS, and `XDG_STATE_HOME` or `~/.local/state` on Linux. Startup prepares and validates the state and recovery roots before recovery runs; the recovery store validates entries before reading them and creates private commit and artifact subdirectories as part of persistence. Direct symbolic links and reparse points are rejected, and Unix directories and files must use `0700` and `0600` respectively. Recovery writes use an explicit private atomic-write contract, leaving repository source-file permissions unchanged. Windows relies on the access controls inherited from the current user's local-application-data directory, consistent with the Host's interactive-user execution model. Explicitly configured directories remain supported and receive the same link and Unix-mode validation. |
| `PRR-F023` | Medium | Open — fix before public publishing | GitHub workflows reference reusable actions by mutable version tags such as `actions/checkout@v6`, `actions/setup-dotnet@v5` and `actions/upload-artifact@v4`. | Pin every reusable action to a reviewed full commit SHA and configure an automated update path such as Dependabot. Keep workflow permissions minimal, and use environment-protected OIDC or trusted publishing rather than long-lived publishing credentials when release workflows are added. |
| `PRR-F024` | Low | Open — robustness | Code Action tokens are sliced, transformed and Base64-decoded without first enforcing the small maximum size implied by the server-generated token contract. Persisted recovery JSON is also read wholly before deserialization. | Reject an oversized Code Action token before allocating or decoding it. After private recovery storage is established, add conservative size bounds for persisted recovery records and artifacts so corruption cannot cause disproportionate startup allocation. |

The remaining dependency order is `PRR-F023`, then `PRR-F024`. Private recovery storage is now established, so recovery-input hardening can proceed after workflow action pinning.

### Accepted residual risks

| Risk | Disposition |
| --- | --- |
| Commit time-of-check/time-of-use window | Target existence and content hashes are revalidated beneath the durable inter-process lock immediately before each atomic replacement. A non-cooperating editor or process can still race the final replacement because the lock coordinates Roslyn Workbench instances rather than every possible filesystem writer. Retain this as documented optimistic coordination and advise callers to avoid external writes during commit. |
| Unbounded filesystem-watcher channel | A sustained event storm can temporarily increase memory while native callbacks outpace the single consumer. The consumer stops after the first relevant change, the native buffer uses its practical maximum, and the existing 80,000-operation and repeated EF Core build evidence did not produce overflow, false staleness or retained growth. Retain the current design unless production telemetry demonstrates a real issue. |
| Detailed local MSBuild and filesystem diagnostics | Workspace-load, project-evaluation and commit diagnostics can contain absolute paths and local tool messages. They are returned only over the caller-owned local stdio channel and are necessary for actionable recovery. Do not add source contents or environment values, and continue returning generic correlated errors for unexpected tool failures. |
| Same-user arbitrary code | A malicious plugin, trusted Workspace build logic, analyzer or another process already running as the same user has the Host's effective permissions. The product does not claim to isolate such code; operating-system or out-of-process sandboxing would be a different architecture. |

## Batch 4 — Product polish and final validation

1. Review public API naming, XML documentation, package metadata and compatibility baselines.
2. Review tool, schema, diagnostic, log and error wording for consistency and actionable recovery guidance.
3. Confirm clean install, first-run, help, version, configuration and plugin-author workflows.
4. Enable appropriate package validation and public API compatibility checks where they provide a stable v1 baseline.
5. Run clean restore, build, latest-all analysis, unit, contract, integration and published-Host acceptance validation.
6. Run the supported Windows and Linux paths and record WSL-specific evidence; treat macOS as best effort only when public release infrastructure exists.
7. Re-run the audit inventory and close, defer with rationale, or promote every finding to an explicit release blocker.

## Completion criteria

The readiness phase is complete when:

- release-facing documents contain no development-only notes or unsupported claims;
- the complete supported tool and lifecycle surface has an evidence-backed disposition;
- security boundaries and residual risks are documented accurately;
- no unresolved critical or high-severity functionality or security finding remains;
- package consumers and the published Host pass clean external validation; and
- remaining work is limited to artifact construction and publication.
